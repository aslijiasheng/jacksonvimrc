server {
    listen       80;
    server_name  jackson.order.com;


    root    /var/www/documents/truecolorpro/orderPlacingMeeting;
    index index.php index.html index.htm;

    if ($request_uri ~* "^(.*/)index\.php$") {
        return 301 $1;
    }

    location / {
        try_files $uri $uri/ /index.php?s=$uri; # 核心
    }

    location /static {
        autoindex on;
    }

    location ~* \.php {  
        fastcgi_split_path_info ^(.+\.php)(.*)$;
        include fastcgi_params;
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_index index.php;
        fastcgi_buffers 16 16k; 
        fastcgi_buffer_size 32k;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }

    error_log  /tmp/log/jackson.order.error.log;
    access_log  /tmp/log/jackson.order.access.log;
}
